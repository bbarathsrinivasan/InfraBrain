# WebSocket URL for the InfraBrain backend
# Set this to connect the frontend to the live backend.
# Leave empty (or remove) to run in frontend-only demo mode (in-browser sim).
VITE_BACKEND_WS=ws://localhost:8002/api/stream
VITE_BACKEND_HTTP=http://localhost:8002
